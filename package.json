{
  "name": "family-live-quiz",
  "version": "1.0.0",
  "private": true,
  "description": "A real-time Hebrew family birthday quiz",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
